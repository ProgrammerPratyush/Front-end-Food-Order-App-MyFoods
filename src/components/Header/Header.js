document.addEventListener('DOMContentLoaded', function() {
    const viewMenuButton = document.getElementById('view-menu-btn');

    viewMenuButton.addEventListener('click', function() {
        // Assuming there's a section or page to navigate to
        window.location.href = '#menu-section'; // Adjust the href as per your menu section or page
    });
});
